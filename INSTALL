Installing sdb-cli
==================

To install, simply run:

  $ make
  $ make install

This will configure the project, compile it, and install it.

You can install in a separate prefix, by setting the "prefix" variable

